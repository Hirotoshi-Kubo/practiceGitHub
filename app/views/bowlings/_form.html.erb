<%= form_with(model: bowling, local: true) do |form| %>
  <% if bowling.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bowling.errors.count, "error") %> prohibited this bowling from being saved:</h2>

      <ul>
      <% bowling.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
